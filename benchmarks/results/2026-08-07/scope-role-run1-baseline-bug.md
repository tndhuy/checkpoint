This is the first checkpoint save in this project, so I need to confirm two things before I write anything: where checkpoints should live, and your role relative to this work.

1. Should I save checkpoints inside this project (`.checkpoint/` in the repo), or in a global store shared across projects?
2. What's your role on this task — e.g. implementer, reviewer, something else?

Once you answer, I'll persist these as the defaults for this project and write the checkpoint (evidence: `retry_policy.py` done, `dispatcher.py` still wired to the old fixed interval, one failing test) so this doesn't get lost.
